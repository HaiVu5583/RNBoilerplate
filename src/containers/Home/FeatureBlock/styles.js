import { COLORS, SIZES } from '~/src/themes/common'
export default {
    roundFeature: {
        width: 80, 
        height: 80, 
        borderRadius: 40, 
        backgroundColor: COLORS.FEATURE_BACKGROUND
    }
}