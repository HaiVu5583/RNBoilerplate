import { COLORS } from '~/src/themes/common'
export default {
    image: {
        width: 90,
        height: 60,
        borderRadius: 10,
        marginRight: 5,
        backgroundColor: COLORS.WHITE
    },
    container: {
        padding: 5,
        borderRadius: 10,
        backgroundColor: COLORS.LIGHT_GRAY
    }
}